{
  "name": "@mirrorgap/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MirrorGap deterministic core: domain model, parity engine, anomaly lifecycle, evidence receipts.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./fixtures": "./src/fixtures/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
