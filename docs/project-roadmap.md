# Roadmap

## Done (hackathon)

- Deterministic parity/dispersion/freshness/market-hours engine
- Event lifecycle + investigation claim ledger + verifiable receipts
- CMC RWA adapter (validated, cached, feature-detected) + fixture mode
- Observatory UI, REST+SSE, CLI, MCP server
- Optional bounded LLM narration (deterministic fallback)

## Next

- [ ] Market-pairs liquidity analysis when plan permits (code path exists, labeled)
- [ ] Historical trend views once enough snapshots accumulate (or CMC adds RWA history)
- [ ] Multi-venue market-hours coverage beyond US equities
- [ ] Public receipt-verification endpoint + hosted read-only instance
- [ ] Postgres store implementation of MirrorGapStore
- [ ] Alerting webhooks on confirmed critical anomalies
