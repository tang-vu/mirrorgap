# Motion scenes

The three scenes share one optical instrument language: mineral paper, blue-black enamel, brushed rails, translucent specimen plates, teal inspection light and vermilion registration marks. All geometry is original HTML/CSS/SVG. The scale, wrapper marks, timestamps, classifications, evidence IDs and receipt values come from the API; no decorative asset encodes a price.

## Preview

From a clean checkout, run `pnpm install` and `pnpm motion:preview`. The command starts its own seeded fixture server on a free loopback port, bundles the vanilla modules in memory with esbuild, and renders them in Edge through a local API bridge. It writes screenshots and the browser report to ignored `data/motion-preview/`. Set `PUPPETEER_EXECUTABLE_PATH` to another Chromium executable if needed. The bridge is only for capture: the shipped site serves the checked-in vanilla modules, CSS and local GSAP files directly.

For the actual HTTP UI, start `node --import tsx src/server.ts` from `apps/web` with `MIRRORGAP_DATA_MODE=fixture`, a disposable `MIRRORGAP_DB_PATH`, `MIRRORGAP_SEED_TICKS=31`, `MIRRORGAP_NO_LOOP=1` and a free `PORT`. Do not point a preview at the host database. The checked-in [desktop opening](ui/motion/opening-desktop.mp4), [desktop journey](ui/motion/journey-desktop.mp4), [mobile opening](ui/motion/opening-mobile.mp4), [mobile journey](ui/motion/journey-mobile.mp4), [desktop seal audit](ui/motion/seal-audit.mp4), [desktop seal tamper](ui/motion/seal-tamper.mp4), [mobile seal audit](ui/motion/seal-audit-mobile.mp4) and [mobile seal tamper](ui/motion/seal-tamper-mobile.mp4) are browser captures from the isolated fixture. The journey storyboard is at [0](ui/motion/journey-000.png), [25](ui/motion/journey-025.png), [50](ui/motion/journey-050.png), [75](ui/motion/journey-075.png) and [100 percent](ui/motion/journey-100.png). The [pass](ui/motion/seal-pass.png) and [fail](ui/motion/seal-fail.png) frames show the returned audit states.

## Choreography

1. The opening timeline lasts two seconds. Rail and plane establish the coordinate system; plates rotate into registration; a light traverses the true marks; the carriage and inspector resolve on the selected wrapper. Asset changes rebuild the scale and orientation deliberately. The ledger and accessible controls remain native DOM.
2. The approximately 300vh journey has one sticky specimen and four normal-scroll chapters. ScrollTrigger scrubs its bracket, specimen, evidence paths, layers and dossier. The scene retains the selected historical snapshot ID and wrapper. Issuance numbers are labeled separately. Background SSE refresh waits while the journey intersects the viewport.
3. The capsule sheet composes actual receipt observations. The audit or verify action traces links while the real request is pending; the returned result drives the stamp and altered slip. Reset restores the original copy. Schema/hash, arithmetic, signature and source attribution remain distinct.

All timelines and observers are disposed by the router cleanup hooks. The opening pauses while hidden or offscreen. Reduced motion retains the evidence and chapter controls without scrubbed animation. Mobile uses the same objects in a shorter sticky stage. GSAP 3.15.0 and ScrollTrigger are installed from npm and served from `apps/web/public/vendor/` under the [GSAP standard no-charge license](https://gsap.com/standard-license/). The apparatus artwork and motion code are original; no reference-site images, video, fonts or models are included.

## Reference access and validation limit

The requested Bearplus, Silana, Heron and United Carriers pages were inspected through the web reader for content. Native Chromium navigation to all four timed out on this machine, including a local HTTP test page, so their moving sequences could not be honestly observed. No visual motion was copied or claimed from them. The local Edge capture did successfully render and record the implemented scenes by using `setContent` and an API bridge. This validates the browser rendering, scroll timeline, interactions and cleanup in that harness. It does not replace a passing normal HTTP `pnpm e2e` run. The latter stopped at its first navigation despite the fixture server returning HTTP 200; rerun it in an environment where Chromium can navigate loopback URLs.

Across the final two Edge captures, apparatus mount from `setContent` to ready took 0.8–2.9 seconds. The browser bundle, CSS and local GSAP payload totaled about 286.6 KB uncompressed. A 60-frame headless sample measured 16.6–16.8 ms median and 18.0–18.1 ms p95 between animation frames. These are fixture capture measurements on this machine, not public page load or field performance. The capture also checked wrapper/carriage/inspector agreement, stable evidence identity on forward and reverse scroll, fast-scroll survival, zero ScrollTriggers after route exit, reduced-motion static layers, and tamper/reset states.
